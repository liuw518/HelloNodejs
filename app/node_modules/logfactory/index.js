var config = require('config');
var log4js = require('log4js');
log4js.configure(config.log4js);
var logger = config.PRODUCTION ? log4js.getLogger('logFile') : log4js.getLogger('console');

module.exports = {
    log4js: log4js,
    getLogger: function () {
        return logger;
    }
};